import { FilterRegistrosPipe } from './filter-registros.pipe';

describe('FilterRegistrosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRegistrosPipe();
    expect(pipe).toBeTruthy();
  });
});
